name: practice_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  cupertino_icons:
  dio:
  flutter:
    sdk: flutter
  flutter_hooks:
  freezed_annotation:
  gap:
  go_router:
  hooks_riverpod:

dev_dependencies:
  build_runner:
  flutter_lints:
  flutter_test:
    sdk: flutter
  freezed:
  json_serializable:

flutter:
  uses-material-design: true
